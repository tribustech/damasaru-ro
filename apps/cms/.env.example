HOST=0.0.0.0
PORT=1337
APP_KEYS="toBeModified1,toBeModified2"
API_TOKEN_SALT=tobemodified
ADMIN_JWT_SECRET=tobemodified
TRANSFER_TOKEN_SALT=tobemodified
JWT_SECRET=tobemodified
ENCRYPTION_KEY=tobemodified

# S3 media upload (aws-s3 provider). Leave AWS_BUCKET blank to keep local-disk uploads.
AWS_ACCESS_KEY_ID=
AWS_ACCESS_SECRET=
AWS_REGION=eu-central-1
AWS_BUCKET=
# Leave AWS_ACL empty for modern buckets (Object Ownership = "Bucket owner
# enforced", AWS default since 2023) — they reject ACLs, so grant public read
# via a bucket policy instead. Set to public-read only on buckets that allow ACLs.
AWS_ACL=

# On-demand revalidation. When content changes, the CMS POSTs the affected cache
# tags to ${WEB_URL}/api/revalidate so Next.js drops the stale pages.
# REVALIDATE_SECRET MUST match the same var in apps/web/.env. Leave WEB_URL or
# REVALIDATE_SECRET blank to disable revalidation (e.g. when web isn't running).
WEB_URL=http://localhost:3000
REVALIDATE_SECRET=
